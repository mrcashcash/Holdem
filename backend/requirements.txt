fastapi==0.116.1
uvicorn[standard]==0.35.0
pydantic==2.11.7
numpy>=1.26,<2.3
numba>=0.61,<0.62
rapidocr>=3.9,<4
onnxruntime>=1.18,<2
resvg_py>=0.3,<0.4
mss>=10.2,<11
windows-capture>=2,<3; platform_system == "Windows" and platform_machine == "AMD64"
# torch is no longer required by the backend (the PPO trainer was retired to
# legacy/); reinstall it only if you want to run the legacy code.
